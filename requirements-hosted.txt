# Slim dependency set for the FREE 512 MB no-card deploy (Render/Koyeb).
#
# The deployed server does NOT run Sentence-BERT locally: query embedding is offloaded to a hosted
# all-mpnet endpoint (see src/retrieval/dense.py, hosted mode via EMBED_API_KEY). That removes
# torch + sentence-transformers -- together ~1 GB of RAM and ~2 GB of wheels -- which is the only
# reason the app fits a free tier. Everything else is identical to the local install.
#
# Deliberately absent vs requirements.txt: sentence-transformers, torch (offloaded), anthropic
# (optional baseline), pytest (dev). pypdf stays because the ingest package imports it at load.

faiss-cpu==1.9.0.post1
rank-bm25==0.2.2
snowballstemmer==2.2.0
numpy==2.1.3
pypdf==6.1.1

httpx==0.28.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
